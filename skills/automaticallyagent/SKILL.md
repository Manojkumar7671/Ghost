---
name: automaticallyagent
description: "Agent that automatically reads and forwards my emails every hour"
tags: ['automatically', 'custom_agent']
triggers: ['automatically reads and forwards my emails every hour', 'use automaticallyAgent']
---

# automaticallyagent Skill Specification

## Overview
Agent that automatically reads and forwards my emails every hour

## Instructions
Generate and execute responses for tasks involving: automatically reads and forwards my emails every hour
