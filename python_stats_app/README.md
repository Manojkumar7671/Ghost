# python_stats_app
A Python application that calculates dataset mean, median, standard deviation and exports JSON report.

## Requirements
- Python 3.x
- numpy
- json

## Usage
1. Install requirements: `pip install -r requirements.txt`
2. Run the application: `python app.py`
3. Find the JSON report in the same directory: `stats_report.json`